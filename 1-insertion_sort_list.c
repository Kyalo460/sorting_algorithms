#include "sort.h"

/**
 * insertion_sort_list - returns without doing anything
 * @list: a double pointer to the linked list
*/
void insertion_sort_list(listint_t **list)
{
	if (*list != NULL)
		return;
	return;
}